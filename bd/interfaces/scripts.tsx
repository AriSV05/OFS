export interface script {
    id: string;
    name: string;
    body: string;
  }

  export interface scriptFront {
    name: string;
    body: string;
  }


export interface scriptUpdate {
    name:string;
  }